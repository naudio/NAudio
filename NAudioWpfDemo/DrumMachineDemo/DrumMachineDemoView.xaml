<UserControl x:Class="NAudioWpfDemo.DrumMachineDemo.DrumMachineDemoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="320" d:DesignWidth="640" xmlns:my="clr-namespace:NAudioWpfDemo.DrumMachineDemo">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal">
            <Button Content="Play" Command="{Binding PlayCommand}" Margin="6" Width="75" />
            <Button Content="Stop" Command="{Binding StopCommand}" Margin="6" Width="75" />
            <TextBlock Margin="6" Text="Tempo:" VerticalAlignment="Center" />
            <Slider Width="100" Value="{Binding Tempo, Mode=TwoWay}" Minimum="60" Maximum="180" Margin="6"/>
            <TextBlock Margin="6" Text="{Binding Tempo, StringFormat='\{0\}bpm'}" VerticalAlignment="Center" />
            <TextBlock Margin="20,6,6,6" Text="Swing:" VerticalAlignment="Center" />
            <Slider Width="100" Value="{Binding Swing, Mode=TwoWay}" Minimum="0" Maximum="0.5" SmallChange="0.05" LargeChange="0.1" Margin="6"
                    ToolTip="Delays odd 16th-note events. 0 = straight, 0.333 ≈ triplet feel, 0.5 = 32nd-note offset."/>
            <TextBlock Margin="6" Text="{Binding Swing, StringFormat='\{0:F2\}'}" VerticalAlignment="Center" />
        </StackPanel>
        <StackPanel Grid.Row="1" Orientation="Horizontal">
            <Button Content="Render to WAV..." Command="{Binding RenderToWavCommand}" Margin="6" Width="120"
                    ToolTip="Renders 8 seconds of the current pattern to a WAV file via the offline sample-driven path." />
        </StackPanel>
        <my:DrumPatternEditor Grid.Row="2" HorizontalAlignment="Left" Margin="20" x:Name="drumPatternEditor1" VerticalAlignment="Top" />
    </Grid>
</UserControl>
