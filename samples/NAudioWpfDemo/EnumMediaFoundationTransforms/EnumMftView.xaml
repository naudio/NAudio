<UserControl x:Class="NAudioWpfDemo.EnumMediaFoundationTransforms.EnumMftView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="263*"/>
        </Grid.RowDefinitions>
        <Button Content="Enumerate" Command="{Binding EnumerateCommand}" Margin="5" Width="80" HorizontalAlignment="Left" />
        <ListView ItemsSource="{Binding Transforms}" Grid.Row="1" Margin="5"></ListView>
    </Grid>
</UserControl>
