<UserControl x:Class="NAudioWpfDemo.FireAndForgetPlayback.FireAndForgetPlaybackView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="500">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        
        <StackPanel Orientation="Horizontal">
            <Button Content="Kick" Command="{Binding KickCommand}" Margin="6" Width="75" />
            <Button Content="Snare" Command="{Binding SnareCommand}" Margin="6" Width="75" />
            <Button Content="Crash" Command="{Binding CrashCommand}" Margin="6" Width="75" />
        </StackPanel>
    </Grid>
</UserControl>
